<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebasestudent">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".Service.MyFirebaseMessingService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>

        </service>

        <activity android:name=".view.view.activity.AddNotification" />
        <activity android:name=".view.view.activity.AddStudent" />
        <activity android:name=".view.view.activity.ParentActivity" />
        <activity android:name=".view.view.activity.TeacherActivity" />
        <activity android:name=".view.view.activity.AddClassActivity" />
        <activity android:name=".view.view.activity.ForgotPassword" />
        <activity android:name=".view.view.activity.ClassesActivity" />
        <activity android:name=".view.view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>